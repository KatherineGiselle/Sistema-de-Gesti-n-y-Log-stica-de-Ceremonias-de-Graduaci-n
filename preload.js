// Espacio reservado para exponer APIs seguras al renderer más adelante,
// por ejemplo cuando se conecte con el backend real de Gerardo (IPC / fetch).
window.addEventListener("DOMContentLoaded", () => {
  // Sin integraciones nativas por ahora: el frontend trabaja con mock-data.js
});
